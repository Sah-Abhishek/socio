
/*

Post{
    user: string,
    content: string
}

*/

// 1. import mongoose
// 2. connect mongoose with the project
// 3. create a schema for the different inputs
// 4. create a model for that schema, it will create a collection in the database
// 5. export the model

// coosnst mongoose = require('mongoose');
// mongoe.connect(process.env.MONGO_URL).then((e) => console.log("mongobd connected"));







// userSchema.index({ userName: 1 }, { unique: true});


 